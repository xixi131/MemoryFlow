# Providers/Inputs

| Event Trigger | State Effect | User-Visible Result | Code Evidence |
| --- | --- | --- | --- |
| Worker `SMTCMonitor` session events (`session-timeline-changed`, `session-media-changed`, `session-playback-changed`) and 1s polling tick call `sendUpdate` | Worker composes normalized payload (`title/artist/status/isPlaying/coverUrl/themeColor/position/duration`) and posts `music-data` to main thread | Island receives fresh metadata/progress/feed updates continuously while media sessions change | `front-end/electron/MusicWorker.cjs:372`, `front-end/electron/MusicWorker.cjs:382`, `front-end/electron/MusicWorker.cjs:338`, `front-end/electron/MusicWorker.cjs:352` |
| Main thread receives worker `music-data` | `MusicService` de-duplicates payloads (`shouldForwardMusicData`) then emits renderer IPC `music-data-update` | Renderer avoids high-frequency duplicate rerenders while still showing metadata/progress changes | `front-end/electron/MusicService.cjs:161`, `front-end/electron/MusicService.cjs:208`, `front-end/electron/MusicService.cjs:213` |
| Renderer subscribes to IPC `music-data-update` | `handleMusicUpdate` updates `musicData` state for `Playing/Paused` payloads | Collapsed/expanded music UI binds to live title, artist, cover, waveform color, and progress data | `front-end/src/components/DynamicIslandWidget.tsx:661`, `front-end/src/components/DynamicIslandWidget.tsx:683`, `front-end/src/components/DynamicIslandWidget.tsx:691`, `front-end/src/components/DynamicIslandWidget.tsx:2145` |
| User clicks prev/play-pause/next buttons (and horizontal trackpad gesture in music mode) | Renderer sends `media-control` IPC; main thread maps command to Windows media virtual keys (177/179/176) | User interaction in island controls system player track/transport state | `front-end/src/components/DynamicIslandWidget.tsx:848`, `front-end/src/components/DynamicIslandWidget.tsx:857`, `front-end/src/components/DynamicIslandWidget.tsx:1524`, `front-end/electron/MusicService.cjs:238`, `front-end/electron/MusicService.cjs:244` |

# Mode Switching

| Event Trigger | State Effect | User-Visible Result | Code Evidence |
| --- | --- | --- | --- |
| `music-data-update` with `status === 'Playing'` or `status === 'Paused'` | `setMusicData(data)`; if switching from app mode, clear compact lock and set `mode='music'` | Island takes over into music activity layout (cover + waveform in collapsed view, full player card when expanded) | `front-end/src/components/DynamicIslandWidget.tsx:683`, `front-end/src/components/DynamicIslandWidget.tsx:691`, `front-end/src/components/DynamicIslandWidget.tsx:695`, `front-end/src/components/DynamicIslandWidget.tsx:704`, `front-end/src/components/DynamicIslandWidget.tsx:2014` |
| First takeover attempt while user is not logged in and current mode is not music | Early return; do not update mode on this event | Island stays in app/login flow instead of auto-switching to music | `front-end/src/components/DynamicIslandWidget.tsx:686`, `front-end/src/components/DynamicIslandWidget.tsx:687`, `front-end/src/components/DynamicIslandWidget.tsx:688` |
| `Paused` payload after entering/remaining in music mode | Start 30s timer; timer callback sets `mode='app'` and clears `musicData` | If playback remains paused, island automatically exits music takeover after grace period | `front-end/src/components/DynamicIslandWidget.tsx:707`, `front-end/src/components/DynamicIslandWidget.tsx:708`, `front-end/src/components/DynamicIslandWidget.tsx:709` |
| `Stopped` payload from worker/service pipeline | Immediately set `mode='app'` and `musicData=null` | Music UI is dismissed immediately and app-mode activity resumes | `front-end/src/components/DynamicIslandWidget.tsx:713`, `front-end/src/components/DynamicIslandWidget.tsx:715`, `front-end/src/components/DynamicIslandWidget.tsx:716` |

# Playback States

| Event Trigger | State Effect | User-Visible Result | Code Evidence |
| --- | --- | --- | --- |
| Worker maps SMTC playback status (`4/Playing`, `5/Paused`, undefined with position stability checks) | Emits normalized `status` plus boolean `isPlaying` in payload | Renderer can consistently show play/pause semantics across players with varying SMTC behavior | `front-end/electron/MusicWorker.cjs:174`, `front-end/electron/MusicWorker.cjs:176`, `front-end/electron/MusicWorker.cjs:178`, `front-end/electron/MusicWorker.cjs:214`, `front-end/electron/MusicWorker.cjs:341` |
| Song timeline updates while `musicData.isPlaying === true` | Local timer increments `localPosition` each second up to `duration`; sync effect corrects drift against server position/title changes | Progress bar advances smoothly; seeks/title switches re-align timeline when drift is detected | `front-end/src/components/DynamicIslandWidget.tsx:761`, `front-end/src/components/DynamicIslandWidget.tsx:768`, `front-end/src/components/DynamicIslandWidget.tsx:825`, `front-end/src/components/DynamicIslandWidget.tsx:830`, `front-end/src/components/DynamicIslandWidget.tsx:2202` |
| `isPlaying` toggles between true/false in payload | Waveform animation and central control icon react to current state | User sees animated waveform + pause icon while playing, static bars + play icon while paused | `front-end/src/components/DynamicIslandWidget.tsx:133`, `front-end/src/components/DynamicIslandWidget.tsx:2043`, `front-end/src/components/DynamicIslandWidget.tsx:2249` |
| Cover/theme fields update in payload | `themeColor` falls through to `musicData?.themeColor || '#22d3ee'`; cover URL binds to squircle image component | Music card tint/waveform color tracks extracted theme; cover thumbnail updates per track | `front-end/src/components/DynamicIslandWidget.tsx:1502`, `front-end/src/components/DynamicIslandWidget.tsx:2029`, `front-end/src/components/DynamicIslandWidget.tsx:2152` |

# Degraded Fallback

| Event Trigger | State Effect | User-Visible Result | Code Evidence |
| --- | --- | --- | --- |
| Media key channel is not ready yet (warm-up/race) when control command arrives | `sendMediaKey` falls back to one-shot encoded PowerShell command | First-click playback controls still respond instead of being dropped during channel startup | `front-end/electron/MusicService.cjs:185`, `front-end/electron/MusicService.cjs:186`, `front-end/electron/MusicService.cjs:188`, `front-end/electron/MusicService.cjs:131` |
| Worker cannot load/extract palette from artwork (`node-vibrant` unavailable or extraction error) | Keep previous/default color (`#22d3ee`) instead of failing payload build | Island keeps stable accent/waveform color instead of crashing on theme extraction | `front-end/electron/MusicWorker.cjs:35`, `front-end/electron/MusicWorker.cjs:41`, `front-end/electron/MusicWorker.cjs:79`, `front-end/electron/MusicWorker.cjs:106` |
| No active media session found | Worker emits explicit `Stopped` payload and resets cached cover/title state | Renderer exits music mode promptly and returns to app view | `front-end/electron/MusicWorker.cjs:133`, `front-end/electron/MusicWorker.cjs:136`, `front-end/electron/MusicWorker.cjs:153`, `front-end/src/components/DynamicIslandWidget.tsx:713` |
| Missing cover art in payload (`coverUrl` empty) | `SquircleCoverThumb` shows placeholder icon instead of image | User still sees a consistent music card with `music_note` placeholder art | `front-end/electron/MusicWorker.cjs:343`, `front-end/src/components/DynamicIslandWidget.tsx:549`, `front-end/src/components/DynamicIslandWidget.tsx:551` |
| Renderer cannot access Electron IPC bridge | Music listener setup falls into catch branch and logs warning | Widget continues running without music takeover instead of crashing render tree | `front-end/src/components/DynamicIslandWidget.tsx:665`, `front-end/src/components/DynamicIslandWidget.tsx:729` |
