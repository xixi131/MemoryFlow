# Phase 5 Interaction And Mock-State Acceptance

Scope: Phase 5 uses mock data and native interaction intents to prove island state, derived visual output, and gesture behavior without real backend calls or real music provider integration.

## Evidence Index

| Evidence | Status | Use in this document |
| --- | --- | --- |
| [`scenario-matrix.json`](evidence/mac-island-phase5/scenario-matrix.json) | Passed after repair | Generated scenario rows include `domainState`, `derivedState`, `visualState`, width data, preview-content metadata, and expected scenario-selection transition metadata for 10 Phase 5 mock scenarios. |
| [`interaction-sequences.json`](evidence/mac-island-phase5/interaction-sequences.json) | Passed after repair | Generated reducer rows include hover, tap, pointer swipe, vertical trackpad, horizontal music, mode switch, reminder, paused-timeout, rapid-retargeting, and explicit click-through, gesture-lock, presentation-lock, and cooldown guard outcomes. |
| [`preview-marker-layout-probe.json`](evidence/mac-island-phase5/preview-marker-layout-probe.json) | Passed after repair | Records compact/hover notch-sized visible widths, activity content-extension routing, and top-attached synthetic sizing so compact no longer inflates into a full-width activity shell. |
| [`native-preview-scenario-e2e.json`](evidence/mac-island-phase5/native-preview-scenario-e2e.json) | Passed after repair with fallback evidence | Records status-menu scenario selections for logged-out, review activity, todo activity, music activity, expanded app, and expanded music. Rows show shell-shape changes, preview-content changes, music cover/waveform placeholders, expanded mock structures, and synthetic notch attachment; physical GUI capture was unavailable. |
| [`native-preview-mouse-e2e.json`](evidence/mac-island-phase5/native-preview-mouse-e2e.json) | Passed after repair with fallback evidence | Records matching interaction-demo fallback checks for hover shadow growth, tap/tap expand-collapse without hover leave, pointer drag compact/restore, and synthetic notch attachment; physical GUI capture was unavailable. |
| [`native-preview-trackpad-e2e.json`](evidence/mac-island-phase5/native-preview-trackpad-e2e.json) | Passed after repair with fallback evidence | Records matching interaction-demo fallback checks for vertical open/expand/close/compact behavior, horizontal mock music commands, app-mode horizontal ignore behavior, and synthetic notch attachment; physical GUI capture was unavailable. |
| Real-device hover, pointer, and trackpad capture | Real-device pending | Phase 5 remains mock-driven; real-device calibration and visible capture still need a supported host environment. |

## State Model

Each state-model row links back to the Phase 5 migration plan and the Windows-derived state evidence so native work can preserve baseline behavior before adding real data.

| Field | Acceptance expectation | Status | Generated evidence | Phase 5 source | Baseline source |
| --- | --- | --- | --- | --- | --- |
| `authState` | Supports `loggedOut` and `loggedIn`; logged-out compact remains a login-oriented branch and logged-in app state can expose app activity sources. | Passed | [`scenario-matrix.json`](evidence/mac-island-phase5/scenario-matrix.json) records `domainState.authState` across logged-out and logged-in scenario rows. | [`Phase 5`](../灵动岛迁移方案.md#phase-5状态机交互意图和-mock-动画场景) | [`Startup/Auth`](mac-island-state-spec.md#startupauth) |
| `primaryMode` | Supports `app` and `music`; music activity only derives from mock music mode plus mock music payload. | Passed | [`scenario-matrix.json`](evidence/mac-island-phase5/scenario-matrix.json) records `domainState.primaryMode` for app and music scenario rows. | [`Phase 5`](../灵动岛迁移方案.md#phase-5状态机交互意图和-mock-动画场景) | [`Visual State Machine`](mac-island-state-spec.md#visual-state-machine) |
| `appDisplayMode` | Supports `review` and `todo`; reminder auto-open stays tied to review behavior and does not add a third display mode. | Passed | [`scenario-matrix.json`](evidence/mac-island-phase5/scenario-matrix.json) records review, todo, and reminder rows under `domainState.appDisplayMode`. | [`Phase 5`](../灵动岛迁移方案.md#phase-5状态机交互意图和-mock-动画场景) | [`Review Rules`](mac-island-state-spec.md#review-rules) / [`Todo Rules`](mac-island-state-spec.md#todo-rules) |
| `presentationState` | Supports `collapsed`, `activity`, and `expanded`; tap, collapse, and vertical trackpad intents move between these shapes through reducer output. | Passed | [`scenario-matrix.json`](evidence/mac-island-phase5/scenario-matrix.json) records all three `domainState.presentationState` values; [`interaction-sequences.json`](evidence/mac-island-phase5/interaction-sequences.json) records reducer movement between them. | [`Phase 5`](../灵动岛迁移方案.md#phase-5状态机交互意图和-mock-动画场景) | [`Visual State Machine`](mac-island-state-spec.md#visual-state-machine) |
| `forceCompactMode` | Forces compact visual output while preserving any mock activity source so pointer or trackpad restore can reopen activity. | Passed | [`scenario-matrix.json`](evidence/mac-island-phase5/scenario-matrix.json) records compact and activity force-compact states; [`interaction-sequences.json`](evidence/mac-island-phase5/interaction-sequences.json) records pointer and trackpad compact/restore reducer rows. | [`Phase 5`](../灵动岛迁移方案.md#phase-5状态机交互意图和-mock-动画场景) | [`Visual State Machine`](mac-island-state-spec.md#visual-state-machine) |
| `isHovered` | Tracks hover enter and leave for collapsed hover output; expanded presentation must not shrink solely because hover leaves. | Passed | [`interaction-sequences.json`](evidence/mac-island-phase5/interaction-sequences.json) records `hover-enter-leave` and rapid hover/tap reducer rows with `isHovered` snapshots. | [`Phase 5`](../灵动岛迁移方案.md#phase-5状态机交互意图和-mock-动画场景) | [`Visual State Machine`](mac-island-state-spec.md#visual-state-machine) |
| `isGestureTracking` | Records active pointer or trackpad gesture ownership so tap-sized movement, swipes, and recovery are not mixed in one update. | Passed | [`interaction-sequences.json`](evidence/mac-island-phase5/interaction-sequences.json) records reducer guard outcomes for `gesture-lock` and cooldown ownership. | [`Phase 5`](../灵动岛迁移方案.md#phase-5状态机交互意图和-mock-动画场景) | [`Visual State Machine`](mac-island-state-spec.md#visual-state-machine) |
| `isModeSwitchLocked` | Prevents a duplicate mode-switch intent while the reducer finishes the current presentation update. | Passed | [`interaction-sequences.json`](evidence/mac-island-phase5/interaction-sequences.json) records `presentation-lock` guard evidence with `modeSwitchLocked` and rapid retargeting rows. | [`Phase 5`](../灵动岛迁移方案.md#phase-5状态机交互意图和-mock-动画场景) | [`Visual State Machine`](mac-island-state-spec.md#visual-state-machine) |
| `isForceCompactLocked` | Retains reducer-level force-compact input ordering; the native shell applies the resulting state immediately without a visual transition window. | Passed | [`interaction-sequences.json`](evidence/mac-island-phase5/interaction-sequences.json) records `forceCompactTransitionLocked` in rapid-retargeting guard evidence. | [`Phase 5`](../灵动岛迁移方案.md#phase-5状态机交互意图和-mock-动画场景) | [`Visual State Machine`](mac-island-state-spec.md#visual-state-machine) |
| `isReminderActive` | Simulates due reminder state with mock data and can open review activity only from compact app mode. | Passed | [`scenario-matrix.json`](evidence/mac-island-phase5/scenario-matrix.json) records the `reminder-due` row with `domainState.isReminderActive=true` and `derivedState.showReminder=true`; [`interaction-sequences.json`](evidence/mac-island-phase5/interaction-sequences.json) records the reminder reducer path. | [`Phase 5`](../灵动岛迁移方案.md#phase-5状态机交互意图和-mock-动画场景) | [`Reminder Trigger Rules`](mac-island-state-spec.md#reminder-trigger-rules) |
| `isReminderCollapsing` | Preserves segmented collapse behavior after reminder or activity expanded-close recovery. | Passed | [`scenario-matrix.json`](evidence/mac-island-phase5/scenario-matrix.json) records `domainState.isReminderCollapsing`; [`interaction-sequences.json`](evidence/mac-island-phase5/interaction-sequences.json) records reminder and collapse recovery reducer paths. | [`Phase 5`](../灵动岛迁移方案.md#phase-5状态机交互意图和-mock-动画场景) | [`Visual State Machine`](mac-island-state-spec.md#visual-state-machine) |
| `isGreetingActive` | Allows greeting-specific compact width derivation without introducing real profile or backend reads. | Passed | [`scenario-matrix.json`](evidence/mac-island-phase5/scenario-matrix.json) records `domainState.isGreetingActive` and generated width metadata without backend/profile reads. | [`Phase 5`](../灵动岛迁移方案.md#phase-5状态机交互意图和-mock-动画场景) | [`Startup/Auth`](mac-island-state-spec.md#startupauth) |

## Derived State

| Output | Acceptance expectation | Status | Generated evidence | Phase 5 source | Baseline source |
| --- | --- | --- | --- | --- | --- |
| `hasMusicActivitySource` | True only when `primaryMode` is `music` and mock music data exists. | Passed | [`scenario-matrix.json`](evidence/mac-island-phase5/scenario-matrix.json) records `derivedState.hasMusicActivitySource` true for music activity/expanded rows and false elsewhere. | [`Phase 5`](../灵动岛迁移方案.md#phase-5状态机交互意图和-mock-动画场景) | [`Visual State Machine`](mac-island-state-spec.md#visual-state-machine) |
| `hasAppActivitySource` | True only when `primaryMode` is `app` and mock auth is logged in. | Passed | [`scenario-matrix.json`](evidence/mac-island-phase5/scenario-matrix.json) records `derivedState.hasAppActivitySource` across logged-in app, music, and logged-out rows. | [`Phase 5`](../灵动岛迁移方案.md#phase-5状态机交互意图和-mock-动画场景) | [`Visual State Machine`](mac-island-state-spec.md#visual-state-machine) |
| `hasAnyActivitySource` | True when either mock music or mock app activity source exists, even if force compact is currently hiding activity content. | Passed | [`scenario-matrix.json`](evidence/mac-island-phase5/scenario-matrix.json) records `derivedState.hasAnyActivitySource` true for mock activity-source rows, including compact rows that hide content. | [`Phase 5`](../灵动岛迁移方案.md#phase-5状态机交互意图和-mock-动画场景) | [`Visual State Machine`](mac-island-state-spec.md#visual-state-machine) |
| `showMusicActivity` | Shows mock music activity when a music source exists, the island is not forced compact, and presentation is not a conflicting app-only branch. | Passed | [`scenario-matrix.json`](evidence/mac-island-phase5/scenario-matrix.json) records `derivedState.showMusicActivity` true for `music-activity` and false for compact/expanded non-activity rows. | [`Phase 5`](../灵动岛迁移方案.md#phase-5状态机交互意图和-mock-动画场景) | [`Visual State Machine`](mac-island-state-spec.md#visual-state-machine) |
| `showAppActivity` | Shows mock app activity when an app source exists, force compact is false, and review or todo derived content is available. | Passed | [`scenario-matrix.json`](evidence/mac-island-phase5/scenario-matrix.json) records `derivedState.showAppActivity` true for review/todo activity and reminder rows. | [`Phase 5`](../灵动岛迁移方案.md#phase-5状态机交互意图和-mock-动画场景) | [`Visual State Machine`](mac-island-state-spec.md#visual-state-machine) |
| `showReminder` | Shows review reminder activity only for app review mode, logged-in mock state, and due-reminder simulation. | Passed | [`scenario-matrix.json`](evidence/mac-island-phase5/scenario-matrix.json) records `derivedState.showReminder=true` only for `reminder-due`. | [`Phase 5`](../灵动岛迁移方案.md#phase-5状态机交互意图和-mock-动画场景) | [`Reminder Trigger Rules`](mac-island-state-spec.md#reminder-trigger-rules) |
| `showTodoActivity` | Shows todo activity only for app todo mode with logged-in mock state and todo payload availability. | Passed | [`scenario-matrix.json`](evidence/mac-island-phase5/scenario-matrix.json) records `derivedState.showTodoActivity=true` for `todo-activity`. | [`Phase 5`](../灵动岛迁移方案.md#phase-5状态机交互意图和-mock-动画场景) | [`Todo Rules`](mac-island-state-spec.md#todo-rules) |
| `showAnyActivity` | True when either mock music or mock app activity should be visible; false in compact-only or logged-out states. | Passed | [`scenario-matrix.json`](evidence/mac-island-phase5/scenario-matrix.json) records `derivedState.showAnyActivity` true only for visible activity rows. | [`Phase 5`](../灵动岛迁移方案.md#phase-5状态机交互意图和-mock-动画场景) | [`Visual State Machine`](mac-island-state-spec.md#visual-state-machine) |
| `isActivityVisualState` | True for the activity visual family, including review, todo, and music activity shell outputs. | Passed | [`scenario-matrix.json`](evidence/mac-island-phase5/scenario-matrix.json) records `derivedState.isActivityVisualState` and `visualState.derived` for activity shell outputs. | [`Phase 5`](../灵动岛迁移方案.md#phase-5状态机交互意图和-mock-动画场景) | [`Visual State Machine`](mac-island-state-spec.md#visual-state-machine) |
| `collapsedWidth` | Derives `240` for activity, `220...300` for greeting text, `180` logged out, `160` logged in app compact, and `230` todo compact. | Passed | [`scenario-matrix.json`](evidence/mac-island-phase5/scenario-matrix.json) records `width.collapsedWidth`, `contentExtensionWidth`, and content-width branch data for generated mock scenarios. | [`Phase 5`](../灵动岛迁移方案.md#phase-5状态机交互意图和-mock-动画场景) | [`Visual State Machine`](mac-island-state-spec.md#visual-state-machine) |
| `collapsedCornerRadius` | Produces the radius required by the resolved compact, hover, activity, or expanded visual state. | Prepared | Generated state evidence records visual-state families; runtime visual token inspection remains a native preview follow-up rather than a scenario-matrix field. | [`Phase 5`](../灵动岛迁移方案.md#phase-5状态机交互意图和-mock-动画场景) | [`Visual State Machine`](mac-island-state-spec.md#visual-state-machine) |
| `collapsedCornerSmoothness` | Produces the smoothness value required by the same resolved visual state and does not depend on real data providers. | Prepared | Generated state evidence records visual-state families; runtime visual token inspection remains a native preview follow-up rather than a scenario-matrix field. | [`Phase 5`](../灵动岛迁移方案.md#phase-5状态机交互意图和-mock-动画场景) | [`Visual State Machine`](mac-island-state-spec.md#visual-state-machine) |

## Interaction Intents

Windows baseline thresholds to mirror at Phase 5 start:

| Baseline control | Value | Source |
| --- | --- | --- |
| Tap movement window | `abs(diff) < 10` | [`Pointer tap`](mac-island-interaction-spec.md#pointer-gestures) |
| Pointer swipe threshold | `26` horizontal points | [`Pointer Gestures`](mac-island-interaction-spec.md#pointer-gestures) |
| Trackpad horizontal threshold | `70` accumulated delta | [`Trackpad Gestures`](mac-island-interaction-spec.md#trackpad-gestures) |
| Trackpad vertical threshold | `70` accumulated delta | [`Trackpad Gestures`](mac-island-interaction-spec.md#trackpad-gestures) |
| Trackpad reset window | `160ms` | [`Trackpad Gestures`](mac-island-interaction-spec.md#trackpad-gestures) |
| Trackpad cooldown window | `320ms` | [`Trackpad Gestures`](mac-island-interaction-spec.md#trackpad-gestures) |

| Intent | Acceptance expectation | Status | Reducer evidence | Interaction source | Animation source |
| --- | --- | --- | --- | --- | --- |
| Hover enter | Sets hover state and resolves hover-collapsed output only when the island is not expanded. | Passed | [`interaction-sequences.json`](evidence/mac-island-phase5/interaction-sequences.json) records `hover-enter-leave` with `hoverEnter` -> `hoverCollapsed`. | [`Hover Activation`](mac-island-interaction-spec.md#hover-activation) | [`Hover Motion`](mac-island-animation-spec.md#hover-motion) |
| Hover leave | Clears hover state, restores click-through when safe, and preserves expanded presentation if expanded is active. | Passed | [`interaction-sequences.json`](evidence/mac-island-phase5/interaction-sequences.json) records `hoverLeave` recovery to compact and click-through guard evidence. | [`Hover Activation`](mac-island-interaction-spec.md#hover-activation) | [`Hover Motion`](mac-island-animation-spec.md#hover-motion) |
| Tap expand | Converts tap-sized pointer movement into expanded app or music presentation unless the logged-out login gate applies. | Passed | [`interaction-sequences.json`](evidence/mac-island-phase5/interaction-sequences.json) records `tap-expand-collapse` with `tapExpandedToApp`. | [`Pointer tap`](mac-island-interaction-spec.md#pointer-gestures) | [`Expand/Collapse`](mac-island-animation-spec.md#expandcollapse) |
| Tap collapse | Converts tap or outside-collapse intent from expanded presentation back to activity or compact according to available mock sources. | Passed | [`interaction-sequences.json`](evidence/mac-island-phase5/interaction-sequences.json) records `tapCollapsedToActivity` and expanded collapse recovery paths. | [`Pointer tap`](mac-island-interaction-spec.md#pointer-gestures) | [`Expand/Collapse`](mac-island-animation-spec.md#expandcollapse) |
| Pointer swipe to compact | Maps right swipe from visible activity into `forceCompactMode=true` compact output. | Passed | [`interaction-sequences.json`](evidence/mac-island-phase5/interaction-sequences.json) records pointer-swipe compact reducer output. | [`Pointer Gestures`](mac-island-interaction-spec.md#pointer-gestures) | [`Expand/Collapse`](mac-island-animation-spec.md#expandcollapse) |
| Pointer swipe to activity | Maps left swipe from compact into `forceCompactMode=false` activity output when any mock activity source exists. | Passed | [`interaction-sequences.json`](evidence/mac-island-phase5/interaction-sequences.json) records pointer-swipe restore-to-activity reducer output. | [`Pointer Gestures`](mac-island-interaction-spec.md#pointer-gestures) | [`Expand/Collapse`](mac-island-animation-spec.md#expandcollapse) |
| Trackpad vertical close | Maps upward vertical swipe to close expanded presentation or collapse visible activity into compact. | Passed | [`interaction-sequences.json`](evidence/mac-island-phase5/interaction-sequences.json) records `trackpad-vertical` close/compact rows and cooldown recovery. | [`Trackpad Gestures`](mac-island-interaction-spec.md#trackpad-gestures) | [`Expand/Collapse`](mac-island-animation-spec.md#expandcollapse) |
| Trackpad vertical open | Maps downward vertical swipe to reopen activity from compact or expand visible activity. | Passed | [`interaction-sequences.json`](evidence/mac-island-phase5/interaction-sequences.json) records `trackpad-vertical` open/expand rows and cooldown recovery. | [`Trackpad Gestures`](mac-island-interaction-spec.md#trackpad-gestures) | [`Expand/Collapse`](mac-island-animation-spec.md#expandcollapse) |
| Trackpad horizontal music command | Emits mock previous-track or next-track command metadata only while `primaryMode` is `music`. | Passed | [`interaction-sequences.json`](evidence/mac-island-phase5/interaction-sequences.json) records `horizontal-music` command metadata and app-mode ignore behavior. | [`Trackpad Gestures`](mac-island-interaction-spec.md#trackpad-gestures) | [`Mode Switch`](mac-island-animation-spec.md#mode-switch) |

## Mock Scenarios

The acceptance catalog must cover logged-out compact, logged-in review compact, logged-in todo compact, review activity, todo activity, music activity, expanded music, expanded app, reminder due, paused music timeout, activity-to-compact gesture, compact-to-activity gesture, expanded-collapse recovery, hover enter/leave, rapid tap, and horizontal music command scenarios.

## Window Wiring

Native preview wiring should translate AppKit, SwiftUI, menu, or probe actions into `IslandInteractionIntent` values first, then let derived state feed the existing native visual-state and motion request path. Phase 5 preview mode may own mock state inside the native window controller, but it must leave Phase 4 sizing and motion infrastructure intact.

## Gesture Guards

Gesture handling must keep click-through, hover hit frames, active pointer identity, trackpad reset, trackpad cooldown, mode-switch locks, and force-compact transition locks from producing overlapping reducer transitions. Guard failures should be visible as no-op reducer metadata rather than direct UI mutation.

## Preview Evidence

Evidence can be generated from static document checks, lightweight Swift typecheck probes, or native preview/menu scenarios. Pure docs rows do not require browser, backend, database, or physical-device validation; later frontend tasks should truthfully separate synthetic probe output from real-device hover, pointer, or trackpad capture.

## Presentation Updates

The native shell now applies compact, activity, hover, and expanded layouts directly. No liquid, spring, opacity, blur, or frame interpolation is active.

## Visible Preview Acceptance

The rows below mark the available Phase 5 visible checks after the repair pass. They are accepted as fallback native-preview/menu-probe evidence because the sandboxed worker environment could not launch/capture the GUI host. They are not screenshot or video proof, and they are not physical-device calibration.

| Visible check | Status | Evidence | Captured observation | Limitation |
| --- | --- | --- | --- | --- |
| Required mock scenario selections | Passed after repair with fallback evidence | [`native-preview-scenario-e2e.json`](evidence/mac-island-phase5/native-preview-scenario-e2e.json) | Logged-out compact, review activity, todo activity, music activity, expanded app, and expanded music menu selections each changed shell shape and preview content from the seed state while staying centered on the synthetic notch and top-attached. Music activity records cover plus waveform placeholders; expanded music records title, artist, progress, and control placeholders; expanded app records review/todo mock structure. | `physicalCaptureAvailable=false`; `init.sh` stopped before launch because backend port `8080` was occupied, `xcodebuild` was unavailable with CommandLineTools-only developer tools, and no physical macOS GUI capture path was available. |
| Compact and hover sizing | Passed after repair with fallback evidence | [`preview-marker-layout-probe.json`](evidence/mac-island-phase5/preview-marker-layout-probe.json), [`native-preview-mouse-e2e.json`](evidence/mac-island-phase5/native-preview-mouse-e2e.json) row `hover-shadow-growth` | Compact rows keep content extension at `0` and remain notch-sized; hover uses a small visible/shadow-frame increase instead of switching to an activity-sized shell. | Synthetic sizing evidence only; physical hover calibration remains pending. |
| Mouse hover shadow growth | Passed after repair with fallback evidence | [`native-preview-mouse-e2e.json`](evidence/mac-island-phase5/native-preview-mouse-e2e.json) row `hover-shadow-growth` | Hover enter changed compact to hover output, increased visible/shadow frame widths, set nonzero shadow tokens, and preserved synthetic notch centering/top attachment. | Interaction source is documented as real hover when GUI capture is available; this run used the matching `MF status item > Phase 5 Interaction Demo > Hover Enter` fallback. |
| Mouse tap expand/collapse | Passed after repair with fallback evidence | [`native-preview-mouse-e2e.json`](evidence/mac-island-phase5/native-preview-mouse-e2e.json) row `tap-expand-collapse` | Two tap intents expanded compact review and then collapsed back to review activity because a mock review source remained; `tapLoopDoesNotRequireHoverLeave=true` records that the second tap is accepted without a hover leave/re-enter cycle. | Interaction source is documented as real tap when GUI capture is available; this run used the matching interaction-demo menu fallback. |
| Pointer drag compact/restore | Passed after repair with fallback evidence | [`native-preview-mouse-e2e.json`](evidence/mac-island-phase5/native-preview-mouse-e2e.json) row `pointer-drag-compact-restore` | Pointer swipe intents compacted and restored the review activity shell, leaving the final visible state as activity and preserving synthetic notch attachment. | Interaction source is documented as real pointer drag when GUI capture is available; this run used the matching pointer-swipe menu fallback. |
| Trackpad vertical open/expand | Passed after repair with fallback evidence | [`native-preview-trackpad-e2e.json`](evidence/mac-island-phase5/native-preview-trackpad-e2e.json) row `trackpad-vertical-open-expand` | Downward trackpad intents opened compact review to activity and then expanded activity to the app surface; the final frame remained centered/top-attached on the synthetic notch. | Interaction source is documented as real trackpad swipes when GUI capture is available; this run used the matching trackpad-down menu fallback plus cooldown completion. |
| Trackpad vertical close/compact | Passed after repair with fallback evidence | [`native-preview-trackpad-e2e.json`](evidence/mac-island-phase5/native-preview-trackpad-e2e.json) row `trackpad-vertical-close-compact` | Upward trackpad intents closed expanded app to activity and then compacted the activity shell, preserving synthetic notch attachment. | Interaction source is documented as real trackpad swipes when GUI capture is available; this run used the matching trackpad-up menu fallback plus cooldown completion. |
| Trackpad horizontal music commands | Passed after repair with fallback evidence | [`native-preview-trackpad-e2e.json`](evidence/mac-island-phase5/native-preview-trackpad-e2e.json) row `trackpad-horizontal-music-commands` | Horizontal music gestures emitted mock `previousTrack` and `nextTrack` commands while staying in music activity mode. | Mock command metadata only; no MediaRemote, Apple Music, Spotify, IPC, or real provider command was invoked. |
| Trackpad horizontal app-mode ignore | Passed after repair with fallback evidence | [`native-preview-trackpad-e2e.json`](evidence/mac-island-phase5/native-preview-trackpad-e2e.json) row `trackpad-horizontal-app-mode-ignored` | The same horizontal command path was ignored in app mode, leaving the app activity shell unchanged and attached to the synthetic notch. | Mock reducer/menu evidence only; no backend or provider integration was exercised. |
| Screenshot or video capture | Real-device pending | Environment caveats in the three native-preview e2e JSON files | No screenshot or video artifact is claimed for Phase 5 in this environment. | Requires a supported macOS GUI host and capture path outside the current sandbox constraints. |
| Real provider or backend integration | Out of scope | This document's scope and non-goals | Phase 5 verifies mock scenarios, mock preview content, reducer state, and native preview/menu interaction routing only. | Real backend data, persisted app data, and real music-provider behavior remain later-phase work. |

## Non-Goals

- No backend API calls.
- No MediaRemote, Apple Music, Spotify, or IPC music provider integration.
- No Phase 6 real content migration layouts; Phase 5 mock preview content is preview-only.
- No Phase 7 app-data persistence behavior.
- No Phase 8 real music-provider behavior.
