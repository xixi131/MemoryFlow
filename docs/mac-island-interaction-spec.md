# Hover Activation

| Interaction | Trigger Threshold | Debounce | Recovery Behavior | Evidence |
| --- | --- | --- | --- | --- |
| Pointer enters the island hit area while the widget is collapsed | No numeric threshold; hover is driven by `onMouseEnter` and the `:hover` check used by gesture finalization | None observed in code | `setIsHovered(true)` on enter; `setIsHovered(false)` on leave or when expanding; if the widget is not expanded and no pointer gesture is active, leave restores click-through | [DynamicIslandWidget.tsx](../front-end/src/components/DynamicIslandWidget.tsx#L1065-L1069), [DynamicIslandWidget.tsx](../front-end/src/components/DynamicIslandWidget.tsx#L1896-L1907) |

# Click-Through Toggle

| Interaction | Trigger Threshold | Debounce | Recovery Behavior | Evidence |
| --- | --- | --- | --- | --- |
| Renderer requests `set-ignore-mouse-events` on hover enter/leave and collapse recovery | Guarded by `isExpanded`, active pointer capture, and gesture-tracking refs; click-through is only restored when the widget is not expanded | None observed in code | `mouseenter` disables click-through, `mouseleave` restores it only when not expanded, and `collapseExpanded()` also re-enables click-through after collapsing | [DynamicIslandWidget.tsx](../front-end/src/components/DynamicIslandWidget.tsx#L617-L639), [DynamicIslandWidget.tsx](../front-end/src/components/DynamicIslandWidget.tsx#L1900-L1915), [main.cjs](../front-end/electron/main.cjs#L1329-L1334) |

# Pointer Gestures

| Interaction | Trigger Threshold | Debounce | Recovery Behavior | Evidence |
| --- | --- | --- | --- | --- |
| Pointer swipe right to collapse activity state into compact state | Horizontal delta `diff > 26` (`GESTURE_SWITCH_THRESHOLD`) after `pointerdown -> pointermove -> pointerup`; only applies when `hasAnyActivitySource` and `showAnyActivity` are true | No explicit pointer cooldown timer | Calls `setForceCompactModeWithTransition(true)` and exits the handler before tap logic | [DynamicIslandWidget.tsx](../front-end/src/components/DynamicIslandWidget.tsx#L926-L927), [DynamicIslandWidget.tsx](../front-end/src/components/DynamicIslandWidget.tsx#L1072-L1117) |
| Pointer swipe left to reopen activity state from compact state | Horizontal delta `diff < -26` after captured pointer sequence; guarded by `hasAnyActivitySource` and `forceCompactMode` | No explicit pointer cooldown timer | Calls `setForceCompactModeWithTransition(false)` and exits the handler before tap logic | [DynamicIslandWidget.tsx](../front-end/src/components/DynamicIslandWidget.tsx#L926-L927), [DynamicIslandWidget.tsx](../front-end/src/components/DynamicIslandWidget.tsx#L1112-L1121) |
| Pointer tap to toggle expanded panel | Absolute horizontal delta `Math.abs(diff) < 10` (`TAP_THRESHOLD`) on pointer release | No timer debounce; relies on threshold gate after swipe checks | Calls `toggleExpand()`; gesture state refs are reset in `finalizeGesture`, and click-through is restored when not expanded and no hover remains | [DynamicIslandWidget.tsx](../front-end/src/components/DynamicIslandWidget.tsx#L927-L927), [DynamicIslandWidget.tsx](../front-end/src/components/DynamicIslandWidget.tsx#L1124-L1126), [DynamicIslandWidget.tsx](../front-end/src/components/DynamicIslandWidget.tsx#L1059-L1069) |
| Pointer gesture capture/lock to one active pointer id | `activePointerIdRef` is set on `pointerdown`; `pointermove`/`pointerup` ignore mismatched ids and track `startX`/`lastPointerXRef` as gesture deltas | No timer debounce; identity guard prevents concurrent pointer streams | `finalizeGesture()` always clears `startX`, `lastPointerXRef`, `activePointerIdRef`, and tracking flag; `pointercancel` also releases capture and finalizes | [DynamicIslandWidget.tsx](../front-end/src/components/DynamicIslandWidget.tsx#L1072-L1096), [DynamicIslandWidget.tsx](../front-end/src/components/DynamicIslandWidget.tsx#L1101-L1108), [DynamicIslandWidget.tsx](../front-end/src/components/DynamicIslandWidget.tsx#L1129-L1137) |

# Trackpad Gestures

| Interaction | Trigger Threshold | Debounce | Recovery Behavior | Evidence |
| --- | --- | --- | --- | --- |
| Gesture accumulation window and idle reset for wheel input | `deltaX`/`deltaY` are accumulated into refs each wheel event; reset timer clears both accumulators after `160ms` (`TRACKPAD_GESTURE_RESET_MS`) | Reset timer is restarted on every wheel event to keep multi-event swipes in one window | `clearTrackpadGestureState()` zeroes `trackpadDeltaXRef`/`trackpadDeltaYRef` and clears the reset timer | [DynamicIslandWidget.tsx](../front-end/src/components/DynamicIslandWidget.tsx#L933-L933), [DynamicIslandWidget.tsx](../front-end/src/components/DynamicIslandWidget.tsx#L965-L972), [DynamicIslandWidget.tsx](../front-end/src/components/DynamicIslandWidget.tsx#L1510-L1518) |
| Horizontal two-finger swipe in music mode switches track (`prev`/`next`) | Dominant horizontal movement `absX >= absY` and `absX >= 70` (`TRACKPAD_HORIZONTAL_THRESHOLD`); direction from sign of `trackpadDeltaXRef` | After action, `lockTrackpadGesture()` sets `trackpadGestureLockedRef=true` and enforces `320ms` cooldown before unlocking | Sends media control, clears accumulators, and blocks new trackpad gestures until cooldown expires | [DynamicIslandWidget.tsx](../front-end/src/components/DynamicIslandWidget.tsx#L932-L934), [DynamicIslandWidget.tsx](../front-end/src/components/DynamicIslandWidget.tsx#L974-L983), [DynamicIslandWidget.tsx](../front-end/src/components/DynamicIslandWidget.tsx#L1523-L1530) |
| Vertical swipe up closes expanded panel | Dominant vertical movement `absY > absX` and `absY >= 70`; swipe-up is `trackpadDeltaYRef > 0` | Uses same 320ms lock cooldown via `lockTrackpadGesture()` after action | If expanded, calls `collapseExpanded({ preserveHoverFocus: true })`, then clears accumulators and locks gesture | [DynamicIslandWidget.tsx](../front-end/src/components/DynamicIslandWidget.tsx#L931-L934), [DynamicIslandWidget.tsx](../front-end/src/components/DynamicIslandWidget.tsx#L1533-L1547) |
| Vertical swipe up collapses activity state to compact state | Same vertical threshold; requires `hasAnyActivitySource` and `!forceCompactModeRef.current` while swiping up | Uses same 320ms lock cooldown via `lockTrackpadGesture()` after action | Calls `setForceCompactModeWithTransition(true)`, clears accumulators, and locks gesture | [DynamicIslandWidget.tsx](../front-end/src/components/DynamicIslandWidget.tsx#L1548-L1554), [DynamicIslandWidget.tsx](../front-end/src/components/DynamicIslandWidget.tsx#L974-L983) |
| Vertical swipe down reopens activity state from compact state | Same vertical threshold; requires `!isExpandedRef.current`, `hasAnyActivitySource`, and `forceCompactModeRef.current` while swiping down (`trackpadDeltaYRef < 0`) | Uses same 320ms lock cooldown via `lockTrackpadGesture()` after action | Calls `setForceCompactModeWithTransition(false)`, clears accumulators, and locks gesture | [DynamicIslandWidget.tsx](../front-end/src/components/DynamicIslandWidget.tsx#L1557-L1564), [DynamicIslandWidget.tsx](../front-end/src/components/DynamicIslandWidget.tsx#L974-L983) |
| Vertical swipe down expands panel from activity state | Same vertical threshold; requires `!isExpandedRef.current` and `showAnyActivity` while swiping down | Uses same 320ms lock cooldown via `lockTrackpadGesture()` after action | Calls `toggleExpand()`, clears accumulators, and locks gesture | [DynamicIslandWidget.tsx](../front-end/src/components/DynamicIslandWidget.tsx#L1565-L1570), [DynamicIslandWidget.tsx](../front-end/src/components/DynamicIslandWidget.tsx#L974-L983) |
